# Neural Tangent Kernel (NTK) — Route A (Kernel Baseline)

Pipeline: data → NTK Gram matrix → ridge solve → evaluate → plots.

## Quickstart
1) Create env
```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
